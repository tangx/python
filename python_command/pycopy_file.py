#!/usr/bin/env python# encoding: utf-8#"""@version:  0.0.1@author: TangHsin@license: Apache Licence@mailto: octowhale@github@site: @software: PyCharm Community Edition@python ver: Python 2.7.12@FILE: pycopy_file.py@time: 2016/11/23 21:17"""import osimport sysdef write(src, dest):    # how to copy    # open a file and write to anther    src_content = open(src, 'rb').read()    dest_object = open(dest, 'wb')    dest_object.write(src_content)    dest_object.close()def copy(src, dest):    # check src is exist    if not os.path.exists(src):        print "Error: %s is not exist" % src    if os.path.isdir(src):        print "Error: %s is a directory" % src    src_filename = os.path.basename(src)    # if dest's parent dir is not exit    # without os.path.join, dest_dirname may be a blank.    dest = os.path.join(os.curdir, dest)    dest_dirname = os.path.dirname(dest)    if not os.path.exists(dest_dirname):        print "Error: Directory %s is not exist!" % dest_dirname        sys.exit(1)    # if dest is dir, copy file into it    if os.path.isdir(dest):        dest_file = os.path.join(dest, src_filename)        write(src, dest_file)    # if dest is file, copy file to cover it    elif os.path.isfile(dest):        dest_file = dest        write(src, dest_file)    # if dest is not exist, dest is the new name    else:        dest_file = dest        write(src, dest_file)if __name__ == "__main__":    # copy('src.txt', r'e:\1\2\a\c\d\dest.txt')    copy('src.txt', 'dest.txt')    # copy(r'e:\1\2', r'e:\1\2_2')